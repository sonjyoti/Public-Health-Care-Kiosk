server:
  port: 8080

spring:
  application:
    name: kiosk-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: appointment-service
          uri: lb://kiosk-appointment-service
          predicates:
            - Path=/api/appointments/**

        - id: availability-service
          uri: lb://kiosk-availability-service
          predicates:
            - Path=/api/availability/**

        - id: recommendation-service
          uri: lb://kiosk-recommendation-service
          predicates:
            - Path=/api/recommend/**

        - id: forms-service
          uri: lb://kiosk-forms-service
          predicates:
            - Path=/api/forms/**

      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods:
              -GET
              -POST
              -PUT
              -DELETE
              -PATCH
            allowedHeaders: "*"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
